@model IEnumerable<VETPRO2.Models.ChipIdentification>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AnimalId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MicrochipNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EartagNumber)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AnimalId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MicrochipNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EartagNumber)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
